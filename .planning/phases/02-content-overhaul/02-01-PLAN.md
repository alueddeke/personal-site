---
phase: 02-content-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-content-overhaul/verify-cdn.sh
  - .planning/phases/02-content-overhaul/CONTENT-DRAFT.md
  - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
autonomous: false
requirements: [CONT-01]
user_setup: []

must_haves:
  truths:
    - "A verify-cdn.sh harness exists that asserts each Phase 2 success criterion against the live CDN"
    - "A CONTENTFUL-INVENTORY.md records the current live entry IDs, versions, default locale, and project/experience link arrays"
    - "CONTENT-DRAFT.md contains all drafted copy (hero, both new experience roles, Piano Teacher, Music School SaaS / SongScope / Gist AI cards, skills list, music/EP copy) for Antoni's review"
    - "The heroTagline Symbol field exists on the personalWebsite content type and is activated"
    - "Antoni has explicitly approved CONTENT-DRAFT.md before any downstream CMA write runs"
  artifacts:
    - path: ".planning/phases/02-content-overhaul/verify-cdn.sh"
      provides: "Parameterized CDN assertion harness for all 5 success criteria"
      min_lines: 40
    - path: ".planning/phases/02-content-overhaul/CONTENT-DRAFT.md"
      provides: "All Phase 2 copy for human approval (D-18 gate)"
      min_lines: 60
    - path: ".planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md"
      provides: "Live entry IDs, versions, locale, link arrays captured before any write"
      min_lines: 20
  key_links:
    - from: "heroTagline field"
      to: "personalWebsite content type"
      via: "CMA PUT /content_types/personalWebsite then PUT /activated"
      pattern: "heroTagline"
---

<objective>
Establish the safety scaffolding for the entire content overhaul: capture live Contentful state, build the CDN verification harness, draft ALL copy into a review doc, add and activate the heroTagline content-type field, and gate everything behind Antoni's explicit approval.

Purpose: D-18 makes draft -> approve -> push a HARD checkpoint because CMA writes are live to production and recruiter handoff is imminent. No downstream plan may write content until this plan completes and approval is granted. This plan also performs the only schema change Phase 2 owns (heroTagline field), which has no copy dependency and can safely precede approval.
Output: verify-cdn.sh, CONTENT-DRAFT.md (approved), CONTENTFUL-INVENTORY.md, activated heroTagline field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-overhaul/02-CONTEXT.md
@.planning/phases/02-content-overhaul/02-RESEARCH.md
@.planning/phases/02-content-overhaul/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live Contentful inventory + build the CDN verify harness</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md, .planning/phases/02-content-overhaul/verify-cdn.sh</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 1 read path; Open Questions #1-3; Validation Architecture SC-1..SC-5 curl recipes; Assumptions A1-A4)
    - .planning/phases/02-content-overhaul/02-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements)
    - .claude/settings.local.json (CMA token lives here; read at execution time only, never print/commit the token value)
    - src/contentful.js (CDN read path: space 0mufjfcbiiue, env master, include depth)
  </read_first>
  <action>
    Read the CMA token from .claude/settings.local.json at execution time only (D-02) — never echo it, never write it to any tracked file. GET the live state from api.contentful.com (space 0mufjfcbiiue, env master) and record into CONTENTFUL-INVENTORY.md: (a) the personalWebsite entry id, sys.version, and the default locale confirmed from the content type (expected en-US per A3); (b) the current personalWebsite.experiences link array (ids + each linked experience title/company) to resolve Open Question #1; (c) the current personalWebsite.projects link array (ids + titles) to confirm whether Wild Oasis and My Frontend Lib still exist (A1/CONT-07) and the existing SongScope and Gist AI entry ids + versions (A2 — CONT-06 is a PUT, not a POST); (d) the personalWebsite content type id and sys.version (confirm it is literally personalWebsite per A4). Then write verify-cdn.sh: a parameterized bash script that reads VITE_CONTENTFUL_ACCESS_TOKEN (read-only CDN token from .env) and runs the five SC assertions from 02-RESEARCH.md Validation Architecture — SC-1 heroTagline present and not containing "passionate"; SC-2 Freelance + Risktec experiences present; SC-3 exactly Music School SaaS / SongScope / Gist AI projects and no Wild Oasis / My Frontend Lib and no notResolvable errors array; SC-4 skills contains AWS / React Native / Docker; SC-5 musicBio contains "Why We're Living" and the Spotify album URL. Each assertion prints PASS or FAIL with the offending value. Use python3 for JSON parsing per the RESEARCH recipes. Add a one-second sleep between CMA calls (Pitfall 5 rate limiting).
  </action>
  <verify>
    <automated>bash .planning/phases/02-content-overhaul/verify-cdn.sh; test -s .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md && echo INVENTORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - verify-cdn.sh exists, is executable bash, and runs all five SC checks against cdn.contentful.com without crashing (FAILs are expected at this stage — content not yet written)
    - CONTENTFUL-INVENTORY.md records: personalWebsite entry id + version; default locale (en-US confirmed); current experiences link ids + titles; current projects link ids + titles; SongScope + Gist AI entry ids + versions; content type id + version
    - No CMA token value appears in any tracked file (grep the two created files for the token prefix returns nothing)
  </acceptance_criteria>
  <done>verify-cdn.sh runs all five assertions; CONTENTFUL-INVENTORY.md captures all live IDs/versions/locale/link arrays; token never written to disk.</done>
</task>

<task type="auto">
  <name>Task 2: Add and activate the heroTagline field on the personalWebsite content type</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 4 Add a Field to a Content Type; Pitfall 1 version mismatch; Pitfall 4 activate vs re-publish)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (content type id + version captured in Task 1)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Per D-08 and RESEARCH Pattern 4: GET the personalWebsite content type from api.contentful.com to capture its full current fields array and sys.version. PUT the content type back with ALL existing field definitions PLUS a new field with id heroTagline, name "Hero Tagline", type Symbol, required false, localized false. Contentful does NOT merge — the PUT body must include every existing field or they are erased (RESEARCH anti-pattern Partial PUT body). Then PUT to /content_types/personalWebsite/activated (NOT /published — content types activate, entries publish) with X-Contentful-Version set to the post-PUT version. Do NOT set the field value yet — that waits for approved copy in Plan 04. Append the new content type version to CONTENTFUL-INVENTORY.md. The field id chosen is heroTagline (documenting the Claude's-discretion item).
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/content_types/personalWebsite?access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);ids=[f['id'] for f in d['fields']];assert 'heroTagline' in ids, ids;print('heroTagline ACTIVATED')"</automated>
  </verify>
  <acceptance_criteria>
    - The personalWebsite content type returned by the CDN includes a field with id heroTagline of type Symbol
    - All pre-existing fields (name, bio, skills, contact, pictures, backgrounds, projects, avatar, audioClips, musicBio, teachingBio, experiences) are still present (none erased by the PUT)
    - The content type was activated via /activated (not /published)
    - CONTENTFUL-INVENTORY.md updated with the new content type version
  </acceptance_criteria>
  <done>heroTagline Symbol field exists on the personalWebsite content type, is activated, and all prior fields remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Draft ALL Phase 2 copy into CONTENT-DRAFT.md</name>
  <files>.planning/phases/02-content-overhaul/CONTENT-DRAFT.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-CONTEXT.md (D-09 hero line; D-11 skills list; D-13/D-14 SongScope; D-15 project set; specifics block for Freelance/Risktec bullets and EP/Spotify)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (SongScope Card Synthesis section — verbatim card headline + description + technologies; technologies tag wording guidance)
    - ~/Desktop/Projects/songscope/INTERVIEW_PREP_SONGSCOPE.md (outcome/impact framing for SongScope)
    - ~/Desktop/Projects/songscope/CURRENT_STATE.md (what works + stack for SongScope)
  </read_first>
  <action>
    Write CONTENT-DRAFT.md with every piece of copy a downstream plan will push, each in a clearly labelled section so Antoni can edit inline. Sections: (1) Hero tagline (heroTagline value) — the D-09 proof-led line "Full-stack engineer who ships production software — live SaaS billing systems, AI/ML products, and mobile apps. AWS certified, based in Toronto." plus a note on whether the "Hi, I'm {name}" greeting stays (Claude's discretion — recommend keeping greeting, tagline below it). (2) Freelance Software Engineer experience (title, company "Self-Employed", startDate 2025-08-01, isOngoing true) with markdown bullet lines covering production billing system, rate-locking pattern, Docker deployment, Stripe Connect, zero-downtime deploy (D-06 markdown-in-description). (3) Risktec Software Engineer (startDate 2024-09-01, endDate 2025-08-01) with bullets on portfolio management system, Figma handoffs, Agile, component-library rebuild. (4) Piano Teacher experience entry (folds existing teachingBio content) for DSGN-05. (5) Music School SaaS project card (lead, outcome-first: live client, real Stripe billing, full-stack ownership) with technologies tags. (6) Gist AI updated description + technologies (React, Express, OAuth, Firebase/Firestore) + live link https://gistai-app.netlify.app. (7) SongScope card — use the verbatim outcome-first headline + markdown description + technologies array from 02-RESEARCH.md SongScope Card Synthesis, projectUrl https://songscope-demo.netlify.app with the demo-mode note. (8) Skills array — the full D-11 list (AWS Cloud Practitioner, React Native / Expo, OpenAI API, XGBoost, recommendation systems, Docker, Docker Compose, GitHub Actions CI/CD, DigitalOcean, Nginx, SSL) merged with existing skills from inventory. (9) Music / EP copy — musicBio updated for EP "Why We're Living" with Spotify album link https://open.spotify.com/album/28d3TgbRj5Y0a7URbWWdgc; note MUSC-02 performance photos are retained unchanged. At the top, add a checklist line: "Approve by replying 'approved' or edit any section inline." Do NOT push anything to Contentful in this task.
  </action>
  <verify>
    <automated>grep -q "Why We're Living" .planning/phases/02-content-overhaul/CONTENT-DRAFT.md && grep -q "songscope-demo.netlify.app" .planning/phases/02-content-overhaul/CONTENT-DRAFT.md && grep -q "AWS Cloud Practitioner" .planning/phases/02-content-overhaul/CONTENT-DRAFT.md && grep -q "Risktec" .planning/phases/02-content-overhaul/CONTENT-DRAFT.md && grep -q "Piano Teacher" .planning/phases/02-content-overhaul/CONTENT-DRAFT.md && echo DRAFT_OK</automated>
  </verify>
  <acceptance_criteria>
    - CONTENT-DRAFT.md contains all nine labelled sections (hero, Freelance, Risktec, Piano Teacher, Music School SaaS, Gist AI, SongScope, skills, music/EP)
    - SongScope section matches the 02-RESEARCH.md synthesis (headline + markdown description + technologies + demo URL + demo-mode note)
    - Experience descriptions use markdown bullets (D-06), not prose paragraphs
    - Skills section includes every D-11 item
    - No CMA write occurs in this task
  </acceptance_criteria>
  <done>CONTENT-DRAFT.md holds all approved-pending copy; grep confirms key required strings present; nothing pushed to Contentful.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Antoni approves CONTENT-DRAFT.md (HARD GATE before any CMA write)</name>
  <files>.planning/phases/02-content-overhaul/CONTENT-DRAFT.md</files>
  <action>HUMAN CHECKPOINT (D-18 hard gate). Pause execution and wait for Antoni to review and approve CONTENT-DRAFT.md. Do NOT perform any CMA content entry or asset write (Plans 02-05) until explicit "approved" is received. CMA writes are live to production and recruiter handoff is imminent. See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    CONTENT-DRAFT.md containing all Phase 2 copy (hero tagline, Freelance + Risktec + Piano Teacher experiences, Music School SaaS / Gist AI / SongScope cards, skills list, EP/music copy). The heroTagline field is added and activated on the content type but its value is NOT yet set. No content entry/asset writes have happened.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/02-content-overhaul/CONTENT-DRAFT.md
    2. Read every section. Edit any wording directly in the file (tagline phrasing, bullet specifics, project blurbs, skills order, EP copy).
    3. Confirm the SongScope card framing and the demo-mode note are acceptable for recruiters.
    4. Confirm experience date ranges (Freelance Aug 2025-Present, Risktec Sep 2024-Aug 2025) are correct.
    5. When satisfied, reply "approved". Downstream plans (02-05) will then push this exact copy to Contentful live production via CMA.
  </how-to-verify>
  <verify>
    <human-check>Antoni has read CONTENT-DRAFT.md, made any edits, and replied "approved".</human-check>
  </verify>
  <resume-signal>Reply "approved" to authorize CMA writes, or describe the edits you want first.</resume-signal>
  <done>Antoni has explicitly approved CONTENT-DRAFT.md; CMA writes are now authorized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor shell -> api.contentful.com | CMA token (write access to live production CMS) crosses here |
| repo working tree -> git history / build bundle | token must NOT cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CMA token in .claude/settings.local.json | mitigate | Read token at execution time only (D-02); never echo, never write to CONTENT-DRAFT.md / CONTENTFUL-INVENTORY.md / verify-cdn.sh; verify created files contain no token substring |
| T-02-02 | Information Disclosure | CMA token in build artifact | accept | curl-only at execution time; token never enters any JS bundle (static read-only public site, no backend) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 2 (RESEARCH Package Legitimacy Audit: zero new packages); nothing to verify |
</threat_model>

<verification>
- verify-cdn.sh runs all five SC assertions (expected: FAIL at this stage, content not yet written)
- heroTagline field present and activated on the content type (CDN content type GET shows it)
- CONTENT-DRAFT.md contains all nine copy sections
- CONTENTFUL-INVENTORY.md captures live IDs/versions/locale
- Approval checkpoint reached; no content entry/asset writes performed
</verification>

<success_criteria>
- Live inventory captured; verify harness built; all copy drafted; heroTagline field activated; Antoni's approval granted before any downstream CMA content write.
</success_criteria>

<output>
Create `.planning/phases/02-content-overhaul/02-01-SUMMARY.md` when done. Record: confirmed default locale, personalWebsite entry id + version, SongScope + Gist AI entry ids + versions, current projects/experiences link arrays, whether Wild Oasis / My Frontend Lib still exist, and the activated content type version.
</output>
