---
phase: 02-content-overhaul
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/components/Experiences.jsx
  - src/components/LandingPage.jsx
  - src/components/Music.jsx
  - src/App.jsx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, DSGN-05, CONT-07, MUSC-01]
user_setup: []

must_haves:
  truths:
    - "Experiences.jsx renders the description field through ReactMarkdown so markdown bullets display as a list (not raw '- ' text) (implements D-07 ReactMarkdown render — the minimal render code change Phase 2 owns)"
    - "LandingPage.jsx renders the heroTagline Contentful field as a prop (no hardcoded 'passionate about creating' string remains) (implements D-08 render side — read heroTagline as a prop)"
    - "App.jsx no longer imports or renders the Teaching component (implements D-17 render side — remove the standalone Teaching section from the layout)"
    - "Music.jsx renders the epArtwork EP cover image (sourced from the Contentful epArtwork asset link via an App.jsx prop), guarded so it is hidden when absent"
    - "npm run build exits 0 and eslint passes with zero warnings"
    - "All five Phase 2 success criteria pass via verify-cdn.sh"
  artifacts:
    - path: "src/components/Experiences.jsx"
      provides: "ReactMarkdown render for experience description bullets"
      contains: "ReactMarkdown"
    - path: "src/components/LandingPage.jsx"
      provides: "heroTagline prop render"
      contains: "heroTagline"
    - path: "src/components/Music.jsx"
      provides: "EP artwork image render from the epArtwork prop"
      contains: "epArtwork"
    - path: "src/App.jsx"
      provides: "Teaching removed; heroTagline passed to LandingPage"
      contains: "heroTagline"
  key_links:
    - from: "App.jsx data.fields.heroTagline"
      to: "LandingPage heroTagline prop"
      via: "prop threading"
      pattern: "heroTagline"
    - from: "Experiences.jsx description"
      to: "ReactMarkdown components renderer"
      via: "react-markdown components prop"
      pattern: "ReactMarkdown"
    - from: "App.jsx data.fields.epArtwork"
      to: "Music epArtwork prop -> <img>"
      via: "prop threading + resolved asset url"
      pattern: "epArtwork"
---

<objective>
Land the three minimal JSX render changes Phase 2 owns so the now-live CDN content renders correctly, then verify all five phase success criteria end-to-end.

Purpose: D-07 (ReactMarkdown in Experiences for the markdown bullets written in Plan 02), D-08 (hero tagline as a Contentful-sourced prop — completing CONT-01's render side), MUSC-01 render side (EP artwork image in Music.jsx from the epArtwork asset linked in Plan 04), D-17 (remove the standalone Teaching section — completing DSGN-05's render side). CONT-02/CONT-03 bullets only display correctly once ReactMarkdown is wired; CONT-07's safety (no notResolvable crash) is confirmed by the build/render check. This is the final slice — after it, a recruiter sees the fully refreshed site.
Output: four edited JSX files (Experiences, LandingPage, Music, App), green build/eslint, and all five SC assertions passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-overhaul/02-CONTEXT.md
@.planning/phases/02-content-overhaul/02-PATTERNS.md
@.planning/phases/02-content-overhaul/02-RESEARCH.md
@.planning/phases/02-content-overhaul/verify-cdn.sh

<interfaces>
<!-- Verbatim current state of the three files, from 02-PATTERNS.md (verified against codebase) -->

src/components/Experiences.jsx — current plain-text render to REPLACE (lines 44-48):
  {experience.fields.description && (
    <p className="mt-4 text-gray-700">{experience.fields.description}</p>
  )}
ReactMarkdown import + customRenderer pattern is in src/components/ProjectCard.jsx (lines 2, 15-27, 53-55).

src/components/LandingPage.jsx — current signature (line 3): function LandingPage({ name, profileImage, backgrounds })
  current hardcoded tagline (lines 30-33): "I am a full stack Web Developer passionate about creating projects from end to end."

src/App.jsx — Teaching import (line 8): import Teaching from "./components/Teaching";
  Teaching JSX block (lines 66-69):
    <Teaching pictures={data.fields.pictures} bio={data.fields.teachingBio} />
  LandingPage call site (lines 44-48): name, profileImage, backgrounds props (add heroTagline)
  Music call site (~lines 59-64): bio, pictures, backgroundImage, audioClips props (add epArtwork={data.fields.epArtwork})

src/components/Music.jsx — current signature (line 4): function Music({ bio, pictures, backgroundImage, audioClips })
  Renders bio (<p>), audioClips (HowlerPlayer), and a pictures grid. There is NO artwork image render today — add an epArtwork <img> block. Resolved asset url path: epArtwork.fields.file.url (contentful.js resolves the Asset link via include depth). Guard with optional chaining so the block is hidden when epArtwork is absent (deploy window before Plan 04 publishes the link).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ReactMarkdown rendering to Experiences.jsx</name>
  <files>src/components/Experiences.jsx</files>
  <read_first>
    - src/components/Experiences.jsx (the file being modified — current plain-text render at lines 44-48)
    - src/components/ProjectCard.jsx (the analog — import on line 2, customRenderer on lines 15-27, usage on lines 53-55)
    - .planning/phases/02-content-overhaul/02-PATTERNS.md (Experiences.jsx section — exact replacement block with ul/li/a renderers)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 6 — components prop not renderers; v9 list rendering)
  </read_first>
  <action>
    Mirror the ProjectCard.jsx ReactMarkdown pattern (D-07). Add the import for ReactMarkdown from react-markdown at the top of Experiences.jsx. Define a customRenderer object with handlers for the a, ul, and li tags using the exact Tailwind classes from 02-PATTERNS.md (link text-sky-400 hover:text-sky-600 with target _blank and rel noopener noreferrer; ul list-disc list-inside mt-2 space-y-1; li text-gray-700). Replace the plain-text description block (current lines 44-48) with a div wrapping a ReactMarkdown element that uses the components prop set to customRenderer and renders experience.fields.description. Use the components prop, NOT renderers (react-markdown v9 silently ignores renderers). Keep the existing conditional guard (only render when description exists). No other behavior changes — sorting and date formatting stay as-is.
  </action>
  <verify>
    <automated>grep -q "import ReactMarkdown" src/components/Experiences.jsx && grep -q "components={customRenderer}" src/components/Experiences.jsx && ! grep -q 'className="mt-4 text-gray-700">{experience.fields.description}' src/components/Experiences.jsx && npx eslint src/components/Experiences.jsx --max-warnings=0 && echo EXPERIENCES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Experiences.jsx imports ReactMarkdown and uses the components prop (not renderers)
    - The old plain-text {experience.fields.description} paragraph render is gone
    - customRenderer defines a, ul, and li handlers with the project Tailwind classes
    - eslint passes with zero warnings on the file
  </acceptance_criteria>
  <done>Experiences.jsx renders description via ReactMarkdown; markdown bullets will display as a styled list.</done>
</task>

<task type="auto">
  <name>Task 2: Wire heroTagline prop (App.jsx -> LandingPage.jsx) and remove hardcoded tagline</name>
  <files>src/App.jsx, src/components/LandingPage.jsx</files>
  <read_first>
    - src/components/LandingPage.jsx (signature line 3; hardcoded tagline lines 30-33)
    - src/App.jsx (LandingPage call site lines 44-48)
    - .planning/phases/02-content-overhaul/02-PATTERNS.md (LandingPage.jsx section — target signature + fallback render; App.jsx call-site target)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 7 heroTagline wiring)
  </read_first>
  <action>
    Per D-08 and RESEARCH Pattern 7: in src/App.jsx, add the heroTagline prop to the LandingPage call site, sourced from data.fields.heroTagline (matching the prop-threading style of the other section components). In src/components/LandingPage.jsx, add heroTagline to the destructured props signature, and replace the hardcoded paragraph ("I am a full stack Web Developer passionate about creating projects from end to end.") with a render of the heroTagline prop and a short fallback string (e.g. "Full-stack engineer who ships production software.") guarded with the project's optional-or pattern so the hero is never blank during the deploy window. Keep the surrounding paragraph element and its Tailwind classes. Do not remove the "Hi, I'm {name}" greeting (Claude's discretion per D — keep the greeting, tagline below it).
  </action>
  <verify>
    <automated>grep -q "heroTagline={data.fields.heroTagline}" src/App.jsx && grep -q "heroTagline" src/components/LandingPage.jsx && ! grep -q "passionate about creating" src/components/LandingPage.jsx && npx eslint src/App.jsx src/components/LandingPage.jsx --max-warnings=0 && echo HERO_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx passes heroTagline={data.fields.heroTagline} to LandingPage
    - LandingPage.jsx destructures heroTagline and renders it with a fallback
    - The string "passionate about creating" no longer appears in LandingPage.jsx
    - eslint passes with zero warnings on both files
  </acceptance_criteria>
  <done>Hero tagline is Contentful-sourced; the hardcoded string is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Render the EP artwork in Music.jsx and thread the epArtwork prop from App.jsx</name>
  <files>src/components/Music.jsx, src/App.jsx</files>
  <read_first>
    - src/components/Music.jsx (signature line 4; existing bio/audioClips/pictures render — add the artwork block, no artwork render exists today)
    - src/App.jsx (Music call site ~lines 59-64 — add the epArtwork prop)
    - src/components/ProjectCard.jsx (analog for resolving a Contentful Asset link to fields.file.url in JSX)
    - .planning/phases/02-content-overhaul/02-CONTEXT.md (MUSC-01 EP artwork; MUSC-02 keep performance photos)
  </read_first>
  <action>
    Complete the EP artwork render side of MUSC-01 (the epArtwork Asset field was added/activated in Plan 01 and linked to the cover asset in Plan 04). In src/App.jsx, add epArtwork={data.fields.epArtwork} to the Music call site, matching the prop-threading style of the other props. In src/components/Music.jsx, add epArtwork to the destructured props signature and render an EP cover <img> sourced from epArtwork.fields.file.url, placed above or beside the bio (a single prominent artwork image, not in the pictures grid — leave the pictures performance-photo grid untouched per MUSC-02). Guard the whole artwork block with optional chaining (epArtwork?.fields?.file?.url) so nothing renders and no error occurs when the link is absent (the pre-Plan-04 deploy window). Use Tailwind utility classes consistent with the surrounding Music section (e.g. rounded-lg shadow-lg, a constrained width); include an alt of "Why We're Living EP artwork". Do not alter the audioClips or pictures rendering.
  </action>
  <verify>
    <automated>grep -q "epArtwork={data.fields.epArtwork}" src/App.jsx && grep -q "epArtwork" src/components/Music.jsx && grep -q "epArtwork?.fields?.file?.url\|epArtwork && epArtwork.fields" src/components/Music.jsx && npx eslint src/App.jsx src/components/Music.jsx --max-warnings=0 && echo EP_ART_WIRED</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx passes epArtwork={data.fields.epArtwork} to the Music component
    - Music.jsx destructures epArtwork and renders an <img> from epArtwork.fields.file.url, optional-chain guarded
    - The performance-photo pictures grid and audioClips rendering are unchanged (MUSC-02)
    - eslint passes with zero warnings on both files
  </acceptance_criteria>
  <done>The EP cover artwork renders in the Music section from the Contentful epArtwork asset; absent-link case is safely guarded.</done>
</task>

<task type="auto">
  <name>Task 4: Remove Teaching from App.jsx; build + verify all five success criteria</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (Teaching import line 8; Teaching JSX block lines 66-69)
    - .planning/phases/02-content-overhaul/02-PATTERNS.md (App.jsx Teaching removal section — both the import and JSX block must be removed together to avoid an unused-import eslint warning)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Validation Architecture SC-1..SC-5; DSGN-05 — Teaching.jsx file stays on disk)
    - .planning/phases/02-content-overhaul/verify-cdn.sh (the five SC assertions)
  </read_first>
  <action>
    Per D-17: remove BOTH the import of Teaching (line 8) AND the Teaching JSX block (lines 66-69) from src/App.jsx — removing only one leaves an unused-import eslint warning. Leave src/components/Teaching.jsx on disk (no file deletion). Leave the teachingBio field references nowhere else needed. Then run npx eslint src --max-warnings=0 and npm run build (must exit 0). Finally run verify-cdn.sh and confirm all five SC assertions PASS now that all content (Plans 02-04) is live: SC-1 hero proof-led, SC-2 Freelance+Risktec, SC-3 exactly three projects no dead ones, SC-4 skills, SC-5 music EP + Teaching absent. If any SC fails, the corresponding upstream plan's content did not publish — report which SC failed rather than patching here.
  </action>
  <verify>
    <automated>! grep -qE 'import Teaching|<Teaching' src/App.jsx && npx eslint src --max-warnings=0 && npm run build && bash .planning/phases/02-content-overhaul/verify-cdn.sh && echo ALL_SC_PASS</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contains no `import Teaching` statement and no `<Teaching` JSX tag (a comment mentioning Teaching is acceptable; the grep is `! grep -qE 'import Teaching|<Teaching'`)
    - src/components/Teaching.jsx still exists on disk (not deleted)
    - npx eslint src --max-warnings=0 passes; npm run build exits 0
    - verify-cdn.sh reports PASS for all five success criteria (SC-1 through SC-5)
  </acceptance_criteria>
  <done>Teaching no longer renders; build + eslint green; all five Phase 2 success criteria pass against the live CDN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contentful CDN -> browser bundle | read-only CDN token (intentionally client-visible) crosses here; no write token in any JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | JS bundle | mitigate | Only the read-only VITE_CONTENTFUL_ACCESS_TOKEN ships in the bundle (existing behavior); the CMA token is never referenced in src/ — confirmed by these edits touching no token |
| T-02-04 | Availability | ProjectCard render of resolved projects | mitigate | Build + verify-cdn.sh SC-3 confirms no notResolvable links survive (would crash ProjectCard); Plan 03 unlink-before-delete prevents the broken-link state |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (react-markdown already present) |
</threat_model>

<verification>
- npx eslint src --max-warnings=0 passes
- npm run build exits 0
- verify-cdn.sh: SC-1..SC-5 all PASS
- Manual (optional): npm run dev — hero shows proof-led tagline, experiences show bullets, three project cards, no Teaching section, music EP visible
</verification>

<success_criteria>
- All five ROADMAP Phase 2 success criteria are TRUE: proof-led hero, Freelance+Risktec experiences with bullets, exactly three projects (no dead ones), updated skills, EP music section with no Teaching section anywhere.
</success_criteria>

<output>
Create `.planning/phases/02-content-overhaul/02-05-SUMMARY.md` when done. Record: the four files edited (Experiences, LandingPage, Music, App), eslint/build status, and the verify-cdn.sh result for each of the five success criteria.
</output>
