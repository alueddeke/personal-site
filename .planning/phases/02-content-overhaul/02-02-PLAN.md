---
phase: 02-content-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
autonomous: true
requirements: [CONT-02, CONT-03, DSGN-05]
user_setup: []

must_haves:
  truths:
    - "A Freelance Software Engineer experience entry (Aug 2025-Present) with markdown bullets is published and linked in personalWebsite.experiences"
    - "A Risktec Software Engineer experience entry (Sep 2024-Aug 2025) with markdown bullets is published and linked in personalWebsite.experiences"
    - "A Piano Teacher experience entry is published and linked in personalWebsite.experiences (DSGN-05 data side)"
    - "All three new experiences appear in the CDN response under includes.Entry"
  artifacts:
    - path: ".planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md"
      provides: "Updated with the three new experience entry ids + versions"
      contains: "Freelance"
  key_links:
    - from: "new experience entries"
      to: "personalWebsite.experiences link array"
      via: "CMA PUT personalWebsite with appended Entry links + re-publish"
      pattern: "experiences"
---

<objective>
Push the three approved experience entries to Contentful via CMA: Freelance Software Engineer, Risktec Software Engineer, and Piano Teacher (folding the retired teaching content into Experiences). Each is created with markdown bullets in its description, published, and linked into personalWebsite.experiences.

Purpose: CONT-02 and CONT-03 add the two new roles with outcome-focused bullets; DSGN-05 (data side) creates the Piano Teacher entry so teaching content survives once the standalone Teaching section is removed from the layout in Plan 05. This is the Experiences vertical slice — after this plan the live CDN serves the real career history.
Output: three published experience entries linked from personalWebsite.experiences.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-content-overhaul/02-CONTEXT.md
@.planning/phases/02-content-overhaul/02-RESEARCH.md
@.planning/phases/02-content-overhaul/CONTENT-DRAFT.md
@.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
@.planning/phases/02-content-overhaul/verify-cdn.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create + publish Freelance and Risktec experience entries</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/CONTENT-DRAFT.md (APPROVED copy — use the Freelance and Risktec sections verbatim, including the markdown bullets; only proceed if Plan 01 approval was granted)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 2 Create + Publish a New Entry; Link Shape Conformance en-US locale keys; Pitfall 1 version; Pitfall 5 rate limiting)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (default locale + existing experience ids)
    - .claude/settings.local.json (CMA token — read at execution time only, never print/commit)
  </read_first>
  <action>
    Per RESEARCH Pattern 2 and D-06: for the Freelance role, POST a new entry to /entries with header X-Contentful-Content-Type set to experience and fields under en-US — title "Freelance Software Engineer", company "Self-Employed", startDate "2025-08-01", isOngoing true, description set to the approved markdown bullet block from CONTENT-DRAFT.md (markdown bullet lines covering production billing system, rate-locking pattern, Docker deployment, Stripe Connect, zero-downtime deploy). Capture the new entry id + version, then PUT /entries/{id}/published with X-Contentful-Version. Repeat for the Risktec role — title "Risktec Software Engineer", company per draft, startDate "2024-09-01", endDate "2025-08-01" (no isOngoing), description set to approved Risktec markdown bullets (portfolio management system, Figma handoffs, Agile, component-library rebuild). Use the default locale key confirmed in CONTENTFUL-INVENTORY.md (en-US). Add a one-second sleep between CMA calls. Record both new entry ids + final versions in CONTENTFUL-INVENTORY.md. Do NOT link them yet — linking happens in Task 3.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=experience&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);t=[i['fields'].get('title') for i in d['items']];assert 'Freelance Software Engineer' in t and 'Risktec Software Engineer' in t, t;print('FREELANCE+RISKTEC PUBLISHED')"</automated>
  </verify>
  <acceptance_criteria>
    - Two new published experience entries exist in the CDN: "Freelance Software Engineer" and "Risktec Software Engineer"
    - Each description field contains markdown bullet lines (not a single prose paragraph)
    - Freelance has isOngoing true and startDate 2025-08-01; Risktec has startDate 2024-09-01 and endDate 2025-08-01
    - Both entry ids + versions recorded in CONTENTFUL-INVENTORY.md
  </acceptance_criteria>
  <done>Freelance and Risktec experience entries are published and visible in the CDN with markdown bullets.</done>
</task>

<task type="auto">
  <name>Task 2: Create + publish the Piano Teacher experience entry (DSGN-05 data)</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/CONTENT-DRAFT.md (APPROVED Piano Teacher section)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 2; D-17 — create Piano Teacher experience entry; teachingBio field stays in the model)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (locale + existing experience ids)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Per D-17: POST a new experience entry (X-Contentful-Content-Type set to experience) with en-US fields — title "Piano Teacher" (or the approved title from the draft), company per draft, an appropriate startDate, and description set to the approved Piano Teacher copy folded from the existing teachingBio content. This makes the teaching history appear in the Experiences list so that removing the standalone Teaching section in Plan 05 loses no information. Publish via /entries/{id}/published. Do NOT delete the teachingBio field from the content model (deferred per D-17). Record the new entry id + version in CONTENTFUL-INVENTORY.md. Linking happens in Task 3.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=experience&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);t=[i['fields'].get('title') for i in d['items']];assert any('Piano' in (x or '') for x in t), t;print('PIANO TEACHER PUBLISHED')"</automated>
  </verify>
  <acceptance_criteria>
    - A published Piano Teacher experience entry exists in the CDN
    - Its description carries the teaching content folded from the prior teachingBio
    - The teachingBio field on personalWebsite is NOT deleted (still present in the content model)
    - Entry id + version recorded in CONTENTFUL-INVENTORY.md
  </acceptance_criteria>
  <done>Piano Teacher experience entry is published; teaching content now lives in the Experiences list.</done>
</task>

<task type="auto">
  <name>Task 3: Link all three new experiences into personalWebsite.experiences and re-publish</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 1 Read-Modify-Write; Link Shape Conformance en-US-keyed Entry links; Pitfall 1 version mismatch; anti-pattern Partial PUT body)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (personalWebsite entry id + version; the three new experience ids; existing experiences array)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Per RESEARCH Pattern 1: GET the personalWebsite entry fresh to capture its current sys.version and FULL fields body (Contentful does not merge — the PUT must contain every field). Append the three new experience Entry links to fields.experiences under en-US, each shaped as a Link object with sys.type Link, sys.linkType Entry, and sys.id set to the new entry id. Preserve all existing experience links and every other field untouched. PUT the full entry body with X-Contentful-Version, then PUT /entries/{personalWebsite-id}/published. If a 409 version mismatch occurs, re-GET and retry (Pitfall 1). Add a one-second sleep between calls. Update CONTENTFUL-INVENTORY.md with the new personalWebsite version.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=personalWebsite&include=5&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);exps=d['items'][0]['fields']['experiences'];ents=d['includes']['Entry'];titles=[next((e['fields'].get('title') for e in ents if e['sys']['id']==l['sys']['id']),None) for l in exps];assert 'Freelance Software Engineer' in titles and 'Risktec Software Engineer' in titles, titles;print('EXPERIENCES LINKED')"</automated>
  </verify>
  <acceptance_criteria>
    - The CDN personalWebsite.experiences array resolves to include Freelance, Risktec, and Piano Teacher
    - No existing experience links were dropped (count is prior count + 3)
    - personalWebsite re-published; the CDN response shows the new experiences resolved (no notResolvable errors)
    - personalWebsite new version recorded in CONTENTFUL-INVENTORY.md
  </acceptance_criteria>
  <done>All three new experiences resolve in the CDN under personalWebsite.experiences; entry re-published cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor shell -> api.contentful.com | CMA token (write access to live production CMS) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CMA token in .claude/settings.local.json | mitigate | Read token at execution time only (D-02); never echo, never write to CONTENTFUL-INVENTORY.md; verify created files contain no token substring |
| T-02-03 | Denial of Service | personalWebsite.experiences link array | mitigate | Read-modify-write full body (Pattern 1); never partial PUT (would erase other fields and break the live site); re-publish so the CDN resolves all links |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 2 |
</threat_model>

<verification>
- CDN shows Freelance, Risktec, and Piano Teacher experience entries published
- personalWebsite.experiences resolves all three (plus prior entries) with no notResolvable errors
- Experience descriptions render markdown bullets (verified visually after Plan 05 ReactMarkdown change)
</verification>

<success_criteria>
- Three new experience entries published and linked; SC-2 (Freelance + Risktec present) passes its CDN assertion.
</success_criteria>

<output>
Create `.planning/phases/02-content-overhaul/02-02-SUMMARY.md` when done. Record the three new experience entry ids + versions and the updated personalWebsite version.
</output>
