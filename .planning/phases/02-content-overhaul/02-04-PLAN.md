---
phase: 02-content-overhaul
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
autonomous: false
requirements: [CONT-01, CONT-08, MUSC-01, MUSC-02]
user_setup:
  - service: image-files
    why: "EP artwork must be in the repo before its asset upload (D-04/D-05)"
    files_required:
      - "contentful/ep-artwork/why-we-are-living.jpg"

must_haves:
  truths:
    - "The personalWebsite.heroTagline field holds the approved proof-led tagline and is published to the CDN"
    - "The personalWebsite.skills array includes AWS Cloud Practitioner, React Native / Expo, AI/ML stack items, and Docker/CI-CD tooling (implements D-10 update skills values only + D-11 the required skills list)"
    - "personalWebsite.musicBio references the EP 'Why We're Living' and the Spotify album URL"
    - "personalWebsite.epArtwork links the published EP cover artwork asset and resolves on the CDN (rendered by Music.jsx in Plan 05)"
    - "Existing performance photos (pictures array) are unchanged (MUSC-02)"
  artifacts:
    - path: ".planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md"
      provides: "Updated with EP artwork asset id and final personalWebsite version"
      contains: "heroTagline"
  key_links:
    - from: "approved hero/skills/music copy + EP artwork asset"
      to: "personalWebsite entry fields"
      via: "single full-body CMA PUT + re-publish"
      pattern: "heroTagline"
---

<objective>
Set the approved hero tagline value, update the skills array, and refresh the music section to the EP "Why We're Living" — all on the personalWebsite entry — plus upload and link the EP artwork asset. Performance photos are left untouched.

Purpose: CONT-01 (hero value — the field was added/activated in Plan 01; this sets the value), CONT-08 (skills), MUSC-01 (EP + Spotify), MUSC-02 (keep performance photos). This is the hero/skills/music vertical slice. Because all three live on the single personalWebsite entry, they are applied in one read-modify-write to avoid version churn.
Output: personalWebsite republished with heroTagline, updated skills, updated musicBio, and EP artwork linked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-content-overhaul/02-CONTEXT.md
@.planning/phases/02-content-overhaul/02-RESEARCH.md
@.planning/phases/02-content-overhaul/CONTENT-DRAFT.md
@.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
@.planning/phases/02-content-overhaul/verify-cdn.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the EP artwork file exists in the repo</name>
  <files>contentful/ep-artwork/why-we-are-living.jpg</files>
  <action>HUMAN CHECKPOINT (D-05 file-existence gate). Pause and wait for Antoni to drop the EP artwork file into contentful/ep-artwork/. Do NOT run the EP artwork upload (Task 2) until the file exists — a missing file makes the upload curl fail (RESEARCH Pitfall 6). See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    Nothing yet — this gate verifies the EP artwork image D-05 requires is present before its asset upload. The file does not exist in the repo yet.
  </what-built>
  <how-to-verify>
    1. Drop the EP "Why We're Living" cover artwork into the repo at exactly: contentful/ep-artwork/why-we-are-living.jpg
    2. Confirm with: ls -la contentful/ep-artwork/ — the file must appear with non-zero size.
    3. Reply "artwork ready" once it exists. Do NOT proceed if the file is missing — the asset upload would fail (RESEARCH Pitfall 6).
  </how-to-verify>
  <verify>
    <human-check>The file exists: test -f contentful/ep-artwork/why-we-are-living.jpg</human-check>
  </verify>
  <resume-signal>Reply "artwork ready" when contentful/ep-artwork/why-we-are-living.jpg exists.</resume-signal>
  <done>contentful/ep-artwork/why-we-are-living.jpg exists with non-zero size.</done>
</task>

<task type="auto">
  <name>Task 2: Upload + publish the EP artwork asset via CMA</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 5 Asset Upload Lifecycle; upload.contentful.com hostname; Pitfall 3 process-before-publish; Pitfall 6 missing file)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (locale)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Precondition: test -f contentful/ep-artwork/why-we-are-living.jpg; abort with a clear message if missing (Pitfall 6). Run RESEARCH Pattern 5: (1) POST the binary to https://upload.contentful.com/spaces/0mufjfcbiiue/uploads (upload hostname) with Content-Type application/octet-stream and --data-binary pointing at the file; capture upload id. (2) POST the asset record to api.contentful.com /assets with en-US file referencing the upload via uploadFrom, contentType image/jpeg, fileName why-we-are-living.jpg, title "Why We're Living EP". (3) PUT /assets/{id}/files/en-US/process. (4) Poll GET /assets/{id} until fields.file.en-US.url is non-empty (max 30s). (5) PUT /assets/{id}/published. One-second sleep between calls. Record the EP artwork asset id in CONTENTFUL-INVENTORY.md on its own line using the EXACT canonical key format `EP_ARTWORK_ASSET_ID=<id>` (no spaces, one per line) so downstream verify commands can grep it reliably.
  </action>
  <verify>
    <automated>ID=$(grep -E '^EP_ARTWORK_ASSET_ID=' .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md | tail -1 | cut -d= -f2); test -n "$ID" || { echo "EP_ARTWORK_ASSET_ID not found in inventory"; exit 1; }; curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/assets/$ID?access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);u=d.get('fields',{}).get('file',{}).get('url','');assert u, d;print('EP ASSET OK',u)"</automated>
  </verify>
  <acceptance_criteria>
    - A published EP artwork asset exists with a non-empty fields.file.url
    - The asset was processed (url populated) before publishing
    - The asset id is recorded in CONTENTFUL-INVENTORY.md as `EP_ARTWORK_ASSET_ID=<id>`
  </acceptance_criteria>
  <done>EP artwork asset uploaded, processed, and published with a valid CDN url.</done>
</task>

<task type="auto">
  <name>Task 3: Set heroTagline, update skills, update musicBio + link EP artwork (single read-modify-write)</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/CONTENT-DRAFT.md (APPROVED hero tagline, skills list, and music/EP copy)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 1 Read-Modify-Write; Pitfall 4 activate-then-re-publish so heroTagline reaches the CDN; anti-pattern Partial PUT body; Link Shape Conformance — Asset link shape for EP artwork; Pitfall 1 version)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (personalWebsite entry id + current version; EP artwork asset id from Task 2)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Per RESEARCH Pattern 1 and Pitfall 4, do this as ONE read-modify-write of the personalWebsite entry to avoid version churn. GET the personalWebsite entry fresh for its full fields body + sys.version. In the body: (a) set fields.heroTagline under en-US to the approved proof-led tagline (CONT-01 — the field was activated in Plan 01; setting the value + re-publish is what surfaces it on the CDN per Pitfall 4); (b) set fields.skills under en-US to the approved merged skills array including AWS Cloud Practitioner, React Native / Expo, OpenAI API, XGBoost, recommendation systems, Docker, Docker Compose, GitHub Actions CI/CD, DigitalOcean, Nginx, SSL (CONT-08); (c) update fields.musicBio under en-US to the approved EP copy referencing "Why We're Living" and the Spotify album URL https://open.spotify.com/album/28d3TgbRj5Y0a7URbWWdgc (MUSC-01); (d) set fields.epArtwork under en-US to an Asset link object (sys.type Link, sys.linkType Asset, sys.id set to the EP artwork asset id recorded as EP_ARTWORK_ASSET_ID in CONTENTFUL-INVENTORY.md from Task 2) — the epArtwork field was added/activated on the content type in Plan 01 Task 2, and Music.jsx is wired to render it in Plan 05 (MUSC-01). Leave fields.pictures (performance photos) and all other fields exactly as-is (MUSC-02 — do not touch performance photos). Send the FULL body (no partial PUT — omitted fields are erased). PUT with X-Contentful-Version, then PUT /entries/{id}/published. Retry on 409 (Pitfall 1). One-second sleep between calls. Record the final personalWebsite version in CONTENTFUL-INVENTORY.md.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=personalWebsite&include=2&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);f=d['items'][0]['fields'];h=f.get('heroTagline','');assert h and 'passionate' not in h.lower(), repr(h);sk=' '.join(f.get('skills',[]));assert 'AWS' in sk and 'Docker' in sk, sk;mb=str(f.get('musicBio',''));assert 'Why We' in mb or '28d3TgbRj5Y0a7URbWWdgc' in mb, mb;ep=f.get('epArtwork');assert ep and ep['sys']['linkType']=='Asset' and ep['sys'].get('id'), ('epArtwork link missing', ep);assets={a['sys']['id'] for a in d.get('includes',{}).get('Asset',[])};assert ep['sys']['id'] in assets, ('epArtwork not resolvable', ep['sys']['id']);print('HERO+SKILLS+MUSIC+EPART OK')"</automated>
  </verify>
  <acceptance_criteria>
    - CDN personalWebsite.heroTagline holds the approved tagline and does not contain "passionate"
    - skills array contains AWS Cloud Practitioner, React Native (Expo), Docker, and CI/CD tooling
    - musicBio references "Why We're Living" and the Spotify album URL
    - personalWebsite.epArtwork resolves to the published EP artwork asset (Asset link present in includes.Asset)
    - pictures (performance photos) array is unchanged from the inventory baseline (MUSC-02)
    - The PUT sent the full body (no fields erased); entry re-published
  </acceptance_criteria>
  <done>heroTagline, skills, music EP copy, and the linked EP artwork are live on the CDN; performance photos untouched; entry re-published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor shell -> api.contentful.com / upload.contentful.com | CMA token (write on live production CMS) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CMA token in .claude/settings.local.json | mitigate | Read token at execution time only (D-02); never echo or write to inventory |
| T-02-06 | Tampering | personalWebsite full-body PUT (skills/pictures) | mitigate | Read-modify-write full body; explicitly preserve pictures + all untouched fields (MUSC-02); no partial PUT |
| T-02-07 | Availability | heroTagline not surfacing on CDN | mitigate | Re-publish the entry after setting the value (Pitfall 4 — activate alone is insufficient) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 2 |
</threat_model>

<verification>
- verify-cdn.sh SC-1 (hero proof-led, no "passionate"), SC-4 (skills), SC-5 (music EP) pass
- pictures array length/contents match the Plan 01 inventory baseline
</verification>

<success_criteria>
- SC-1, SC-4, and the music portion of SC-5 pass: hero tagline proof-led, skills updated, EP live; performance photos retained.
</success_criteria>

<output>
Create `.planning/phases/02-content-overhaul/02-04-SUMMARY.md` when done. Record the EP artwork asset id, how the EP artwork is wired into the music section, the final skills array, and the updated personalWebsite version.
</output>
