---
phase: 02-content-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
autonomous: false
requirements: [CONT-04, CONT-05, CONT-06, CONT-07]
user_setup:
  - service: image-files
    why: "New project thumbnails must be dropped into the repo before asset uploads (D-04/D-05)"
    files_required:
      - "contentful/thumbnails/MusicSchool_Thumbnail.png"
      - "contentful/thumbnails/SongScope_Thumbnail.png"
      - "contentful/thumbnails/GistAI_Thumbnail_New.png"

must_haves:
  truths:
    - "Music School SaaS is a published project entry with an outcome-first description, a published thumbnail asset, and is linked as the lead project in personalWebsite.projects"
    - "The Gist AI project entry is updated with new description, technologies, fresh screenshot, and the working live link https://gistai-app.netlify.app"
    - "The SongScope project entry carries the real outcome-first card (not a placeholder) with the demo link and demo-mode note"
    - "Wild Oasis and My Frontend Lib are absent from personalWebsite.projects and absent as entries; the CDN response has no notResolvable errors"
    - "personalWebsite.projects resolves to exactly three projects: Music School SaaS, SongScope, Gist AI"
  artifacts:
    - path: ".planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md"
      provides: "Updated with Music School SaaS entry id, new asset ids, and final projects array"
      contains: "Music School SaaS"
  key_links:
    - from: "project entries + thumbnail assets"
      to: "personalWebsite.projects link array"
      via: "CMA PUT personalWebsite with exactly 3 Entry links + re-publish"
      pattern: "projects"
    - from: "Wild Oasis / My Frontend Lib"
      to: "deleted (unlink -> unpublish -> delete)"
      via: "unlink from projects array FIRST, then unpublish + delete entries"
      pattern: "notResolvable"
---

<objective>
Deliver the final project set live: create Music School SaaS as the lead card, update Gist AI and SongScope to their real content, and hard-remove Wild Oasis and My Frontend Lib using the unlink-before-delete sequence. Upload all required thumbnail assets via the CMA, gated on the image files existing in the repo.

Purpose: CONT-05 (Music School SaaS lead), CONT-04 (Gist AI update), CONT-06 (SongScope real card), CONT-07 (remove dead projects). This is the Projects vertical slice — the section a recruiter judges hardest. The deletion order is safety-critical: deleting while still linked produces a CDN notResolvable error that crashes ProjectCard.jsx (RESEARCH Pitfall 2).
Output: exactly three published, correctly-thumbnailed project cards resolving from personalWebsite.projects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-content-overhaul/02-CONTEXT.md
@.planning/phases/02-content-overhaul/02-RESEARCH.md
@.planning/phases/02-content-overhaul/CONTENT-DRAFT.md
@.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md
@.planning/phases/02-content-overhaul/verify-cdn.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm the three new project thumbnail files exist in the repo</name>
  <files>contentful/thumbnails/MusicSchool_Thumbnail.png, contentful/thumbnails/SongScope_Thumbnail.png, contentful/thumbnails/GistAI_Thumbnail_New.png</files>
  <action>HUMAN CHECKPOINT (D-05 file-existence gate). Pause and wait for Antoni to drop the three new thumbnail files into contentful/thumbnails/. Do NOT run the asset upload (Task 2) until all three files exist — a missing file makes the upload curl fail (RESEARCH Pitfall 6). See what-built / how-to-verify / resume-signal below.</action>
  <what-built>
    Nothing yet — this gate verifies the image files D-05 requires are present before any asset upload runs. Currently the repo has only contentful/thumbnails/GistAI_Thumbnail.png (the OLD screenshot) and contentful/thumbnails/WildOasis_Thumbnail.png. The three NEW files do not exist yet.
  </what-built>
  <how-to-verify>
    1. Drop these three image files into the repo at exactly these paths:
       - contentful/thumbnails/MusicSchool_Thumbnail.png (Music School SaaS card image)
       - contentful/thumbnails/SongScope_Thumbnail.png (SongScope card image)
       - contentful/thumbnails/GistAI_Thumbnail_New.png (FRESH Gist AI screenshot — CONT-04 requires a new one, not the old GistAI_Thumbnail.png)
    2. Confirm with: ls -la contentful/thumbnails/ — all three new filenames must appear with non-zero size.
    3. Reply "files ready" once all three exist. Do NOT proceed past this gate if any file is missing — asset upload would fail with "No such file or directory" (RESEARCH Pitfall 6).
  </how-to-verify>
  <verify>
    <human-check>All three files exist: test -f contentful/thumbnails/MusicSchool_Thumbnail.png && test -f contentful/thumbnails/SongScope_Thumbnail.png && test -f contentful/thumbnails/GistAI_Thumbnail_New.png</human-check>
  </verify>
  <resume-signal>Reply "files ready" when all three thumbnail files exist at the specified paths.</resume-signal>
  <done>All three new thumbnail files exist in contentful/thumbnails/ with non-zero size.</done>
</task>

<task type="auto">
  <name>Task 2: Upload + publish the three project thumbnail assets via CMA</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 5 Asset Upload Lifecycle — 5 steps; upload.contentful.com hostname; poll for fields.file.url before publish; Pitfall 3 process-before-publish; Pitfall 5 rate limiting; Pitfall 6 missing files)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (locale)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Precondition: verify all three files exist (test -f each path) and abort with a clear message if any is missing (Pitfall 6) — do not invent placeholders. For each of contentful/thumbnails/MusicSchool_Thumbnail.png, contentful/thumbnails/SongScope_Thumbnail.png, contentful/thumbnails/GistAI_Thumbnail_New.png, run RESEARCH Pattern 5 exactly: (1) POST the binary to https://upload.contentful.com/spaces/0mufjfcbiiue/uploads (NOTE the upload hostname, not api) with Content-Type application/octet-stream and --data-binary pointing at the file path; capture upload id. (2) POST an asset record to api.contentful.com /assets with en-US file referencing the upload via uploadFrom link, correct contentType (image/png) and fileName. (3) PUT /assets/{id}/files/en-US/process with X-Contentful-Version. (4) Poll GET /assets/{id} every 2 seconds (max 30s) until fields.file.en-US.url is a non-empty string (Pitfall 3 — never publish before the url exists). (5) PUT /assets/{id}/published with the final version. Add a one-second sleep between CMA calls (Pitfall 5). Record all three asset ids in CONTENTFUL-INVENTORY.md.
  </action>
  <verify>
    <automated>for ID in $(grep -oE 'asset[_ ]id[^a-zA-Z0-9]*[A-Za-z0-9]+' .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md | grep -oE '[A-Za-z0-9]+$'); do curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/assets/$ID?access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);u=d.get('fields',{}).get('file',{}).get('url','');assert u, d;print('ASSET OK',u)"; done</automated>
  </verify>
  <acceptance_criteria>
    - Three published assets exist, each with a non-empty fields.file.url served from the Contentful asset CDN
    - No asset was published before its url field was populated (process step completed first)
    - The Gist AI asset uses GistAI_Thumbnail_New.png (the fresh screenshot), not the old GistAI_Thumbnail.png
    - All three asset ids recorded in CONTENTFUL-INVENTORY.md
  </acceptance_criteria>
  <done>Three thumbnail assets uploaded, processed, and published with valid CDN urls.</done>
</task>

<task type="auto">
  <name>Task 3: Create Music School SaaS (lead) + update Gist AI and SongScope entries</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/CONTENT-DRAFT.md (APPROVED Music School SaaS, Gist AI, and SongScope copy + technologies tags + live links)
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 1 update existing entry; Pattern 2 create new entry; SongScope Card Synthesis; Link Shape Conformance — Asset thumbnail link shape; anti-pattern Partial PUT body; Pitfall 1 version)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (existing SongScope + Gist AI entry ids + versions from Plan 01; the three asset ids from Task 2)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    (a) Music School SaaS (CONT-05, create): POST a new project entry (X-Contentful-Content-Type set to project) with en-US fields — title "Music School SaaS", description set to the approved outcome-first copy (live client, real Stripe billing, full-stack ownership), technologies set to the approved tag array, projectUrl per draft, and thumbnail set to an Asset link to the Music School asset id from Task 2. Publish via /entries/{id}/published. (b) Gist AI (CONT-04, update): GET the existing Gist AI entry (id from inventory) for its full body + version, PUT the FULL body with updated description, technologies set to React, Express, OAuth, Firebase/Firestore, projectUrl https://gistai-app.netlify.app, and thumbnail re-pointed to the new GistAI_Thumbnail_New asset id; re-publish. (c) SongScope (CONT-06, update): GET the existing SongScope entry, PUT the FULL body with the verbatim SongScope Card Synthesis content from 02-RESEARCH.md (outcome-first headline + markdown description), technologies set to Django, Next.js 14, TypeScript, PostgreSQL, Spotify API, OpenAI, Python, Recharts, React, projectUrl https://songscope-demo.netlify.app, thumbnail re-pointed to the SongScope asset id; re-publish. Always GET fresh before each PUT for the current version (Pitfall 1); always send the full fields body (no partial PUT). One-second sleep between calls. Record the new Music School SaaS entry id in CONTENTFUL-INVENTORY.md.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=project&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);t=[i['fields'].get('title') for i in d['items']];u=[i['fields'].get('projectUrl') for i in d['items']];assert 'Music School SaaS' in t, t;assert 'https://gistai-app.netlify.app' in u, u;assert 'https://songscope-demo.netlify.app' in u, u;print('PROJECTS UPDATED')"</automated>
  </verify>
  <acceptance_criteria>
    - Music School SaaS project entry exists, is published, has the outcome-first description and a thumbnail Asset link
    - Gist AI entry has technologies React/Express/OAuth/Firebase-Firestore, projectUrl https://gistai-app.netlify.app, and the fresh thumbnail
    - SongScope entry matches the 02-RESEARCH.md synthesis (headline + markdown description + technologies + demo URL + demo-mode note)
    - Each PUT sent the full fields body (no fields erased)
  </acceptance_criteria>
  <done>Music School SaaS created; Gist AI and SongScope updated to real content; all three published with thumbnails.</done>
</task>

<task type="auto">
  <name>Task 4: Unlink, unpublish, and delete Wild Oasis + My Frontend Lib; set final projects array</name>
  <files>.planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md</files>
  <read_first>
    - .planning/phases/02-content-overhaul/02-RESEARCH.md (Pattern 3 Delete unlink->unpublish->delete; Pitfall 2 broken links / notResolvable crash; Open Question #2 + Assumption A1 — they may already be deleted; Pattern 1 Read-Modify-Write)
    - .planning/phases/02-content-overhaul/CONTENTFUL-INVENTORY.md (current projects link array + Wild Oasis / My Frontend Lib entry ids from Plan 01, and the Music School SaaS id from Task 3)
    - .claude/settings.local.json (CMA token — read at execution time only)
  </read_first>
  <action>
    Per D-16 and RESEARCH Pattern 3, ORDER IS CRITICAL. First check inventory: STATE.md claims Wild Oasis + My Frontend Lib were already deleted (A1) — if a CMA GET of each entry id returns 404 and they are absent from personalWebsite.projects, skip deletion and proceed to the array reconciliation. Otherwise: (1) UNLINK FIRST — GET the personalWebsite entry fresh, build the final fields.projects under en-US containing EXACTLY the three Entry links Music School SaaS, SongScope, Gist AI (lead order: Music School SaaS first per D-15) and excluding Wild Oasis + My Frontend Lib; PUT the full personalWebsite body with X-Contentful-Version and re-publish. (2) Only AFTER the unlink is published, unpublish each dead entry via DELETE /entries/{id}/published with its version. (3) Then DELETE /entries/{id} for each. Never delete before unlinking — a still-linked deleted entry yields a CDN notResolvable error that crashes ProjectCard.jsx (Pitfall 2). One-second sleep between calls. Record the final projects array + new personalWebsite version in CONTENTFUL-INVENTORY.md.
  </action>
  <verify>
    <automated>curl -s "https://cdn.contentful.com/spaces/0mufjfcbiiue/environments/master/entries?content_type=personalWebsite&include=5&access_token=$VITE_CONTENTFUL_ACCESS_TOKEN" | python3 -c "import sys,json;d=json.load(sys.stdin);assert 'errors' not in d or not d['errors'], d.get('errors');projs=d['items'][0]['fields']['projects'];ents=d['includes']['Entry'];titles=[next((e['fields'].get('title') for e in ents if e['sys']['id']==l['sys']['id']),None) for l in projs];assert set(titles)=={'Music School SaaS','SongScope','Gist AI'}, titles;print('PROJECTS FINAL', titles)"</automated>
  </verify>
  <acceptance_criteria>
    - personalWebsite.projects resolves to exactly Music School SaaS, SongScope, Gist AI (Music School SaaS lead/first)
    - Wild Oasis and My Frontend Lib are absent from the projects array AND their entries return 404 on CMA GET
    - The CDN response contains no errors array / no notResolvable items
    - Unlink was published BEFORE any entry deletion (no broken-link window)
  </acceptance_criteria>
  <done>Exactly three projects resolve in the CDN; dead projects fully removed; no notResolvable errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor shell -> api.contentful.com / upload.contentful.com | CMA token (write/delete on live production CMS) crosses here |
| repo image files -> Contentful asset CDN | binary uploads cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CMA token in .claude/settings.local.json | mitigate | Read token at execution time only (D-02); never echo, never write to inventory; created files contain no token substring |
| T-02-04 | Denial of Service | personalWebsite.projects after delete | mitigate | Unlink-before-delete ordering (Pattern 3); verify CDN has no notResolvable errors before deleting entries; full-body PUT (no partial) |
| T-02-05 | Tampering | asset upload (wrong hostname / unprocessed publish) | mitigate | Upload to upload.contentful.com; poll for fields.file.url before publish (Pitfall 3) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Phase 2 |
</threat_model>

<verification>
- Run verify-cdn.sh SC-3 assertion: exactly three projects, no Wild Oasis / My Frontend Lib, no notResolvable
- Each project card has a resolvable thumbnail url
- Gist AI and SongScope live links match the approved URLs
</verification>

<success_criteria>
- SC-3 passes: Projects section resolves to exactly Music School SaaS, SongScope, Gist AI with valid thumbnails and live links; dead projects gone with no broken links.
</success_criteria>

<output>
Create `.planning/phases/02-content-overhaul/02-03-SUMMARY.md` when done. Record: Music School SaaS entry id, three asset ids, final projects link order, deletion status of Wild Oasis / My Frontend Lib (deleted now vs already absent), and the updated personalWebsite version.
</output>
