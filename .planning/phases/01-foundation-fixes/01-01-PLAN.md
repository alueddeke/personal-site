---
phase: 01-foundation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NavBar.jsx
  - src/components/ContactMe.jsx
  - src/components/Footer.jsx
  - src/App.jsx
  - index.html
autonomous: false
requirements: [BUG-01, BUG-02, BUG-03, BUG-05, BUG-06, BUG-07, BUG-08, PERF-03]

must_haves:
  truths:
    - "On a phone-width viewport, a hamburger icon appears far-right in the navbar; tapping it reveals a stacked list of nav links; tapping a link scrolls to that section and closes the menu"
    - "The ContactMe section renders both a form (left column) and a contact-info block (right column) side-by-side on desktop and stacked on mobile"
    - "Pasting the site URL in Slack/LinkedIn shows a preview with title 'Antoni Lueddeke — Software Engineer' and the locked description"
    - "The browser console shows zero log output on page load and on contact-form submit"
    - "The footer shows the current year computed at render time, not a hardcoded 2024"
    - "The EmailJS hidden to_name input carries value 'Antoni Lueddeke'"
  artifacts:
    - path: "src/components/NavBar.jsx"
      provides: "Mobile hamburger toggle + slide-down dropdown"
      contains: "isMenuOpen"
    - path: "src/components/ContactMe.jsx"
      provides: "Two-column contact layout (form + contact info), no console.log, correct hidden input"
      contains: "md:w-1/2"
    - path: "src/components/Footer.jsx"
      provides: "Dynamic copyright year"
      contains: "getFullYear"
    - path: "src/App.jsx"
      provides: "Contentful fetch without debug logging"
    - path: "index.html"
      provides: "Title, description, OG and Twitter Card meta tags"
      contains: "og:title"
  key_links:
    - from: "src/components/NavBar.jsx mobile buttons"
      to: "scrollToSection + setIsMenuOpen(false)"
      via: "onClick handler calling both"
      pattern: "setIsMenuOpen\\(false\\)"
    - from: "index.html <head>"
      to: "social scrapers"
      via: "static meta property tags"
      pattern: "property=\"og:"
---

<objective>
Make the personal website clean, navigable on mobile, and shareable with correct link previews — all via in-place surgical edits to five existing files. After this plan, a recruiter on a phone can navigate every section, the contact section renders both columns, the URL unfurls correctly when shared, and the production console is silent.

Purpose: This is the "navigable and professional" vertical slice — every locally-fixable foundation bug shipped together so the site stops being embarrassing before design work begins.
Output: Edited NavBar.jsx, ContactMe.jsx, Footer.jsx, App.jsx, index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fixes/01-CONTEXT.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md
@.planning/phases/01-foundation-fixes/01-PATTERNS.md

<interfaces>
<!-- Key existing patterns the executor must replicate. From codebase inspection. -->

NavBar.jsx existing state + helpers (extend, do not rewrite):
- `const [isScrolled, setIsScrolled] = useState(false);`
- `const [showSkills, setShowSkills] = useState(false);`
- `const scrollToSection = (sectionId) => { document.getElementById(sectionId)?.scrollIntoView({ behavior: "smooth" }); }`
- `linkClass` (line ~47): reusable nav button className string keyed on `isScrolled`

External link safe pattern (from ProjectCard.jsx:21-22, 97-98 — already compliant):
- `target="_blank" rel="noopener noreferrer"`

Vite env pattern (do NOT change): `import.meta.env.VITE_EMAILJS_*` in ContactMe.jsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile hamburger navigation to NavBar (BUG-01)</name>
  <files>src/components/NavBar.jsx</files>
  <read_first>
    - src/components/NavBar.jsx (current state — see existing useState/scrollToSection/linkClass and the desktop `hidden md:block` nav at lines ~69-116)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (NavBar section — exact insertion points and the established `block md:hidden` vs `hidden md:block` responsive pattern)
  </read_first>
  <action>
    Add a third useState `const [isMenuOpen, setIsMenuOpen] = useState(false);` alongside the existing isScrolled/showSkills state (per D-01: toggle state lives in NavBar.jsx, no new Drawer component). Inside the `flex items-center justify-between h-16` row, after the avatar block, add a hamburger toggle button with className `block md:hidden ml-auto p-2`, `aria-label="Toggle menu"`, onClick `() => setIsMenuOpen(!isMenuOpen)`, rendering `✕` when open and `☰` when closed (per D-03: far-right position; icon glyph is Claude's discretion). After the existing desktop nav wrapper closes but still inside `<nav>`, add a conditionally-rendered (`isMenuOpen && ...`) slide-down dropdown with className `block md:hidden bg-white border-t border-gray-200` (per D-02: theme-neutral functional styling, no mint/teal investment — Phase 3 restyles). Inside it stack a `flex flex-col` list of buttons reusing the existing `linkClass` for: About Me → "about", Experiences → "experiences", Music → "music", Projects → "projects", Contact Me → "contact". Each button's onClick MUST call BOTH `scrollToSection(id)` AND `setIsMenuOpen(false)` (Pitfall 1 — menu must close on navigation). Omit the hover-only Skills dropdown from the mobile list (it relies on onMouseEnter and has no mobile equivalent). Do not modify the existing desktop nav.
  </action>
  <verify>
    <automated>grep -c "setIsMenuOpen(false)" src/components/NavBar.jsx | grep -qx 5 && grep -q "block md:hidden" src/components/NavBar.jsx && npm run lint 2>&1 | grep -v node_modules | grep -i "NavBar.*error" | wc -l | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - NavBar.jsx contains `const [isMenuOpen, setIsMenuOpen] = useState(false)`
    - NavBar.jsx contains a button with className including `block md:hidden` and `aria-label="Toggle menu"`
    - `grep -c "setIsMenuOpen(false)" src/components/NavBar.jsx` returns 5 (one per mobile nav link)
    - The mobile dropdown is wrapped in `{isMenuOpen && (...)}` conditional render
    - On a viewport < 768px, the hamburger glyph is visible far-right in the navbar; clicking it reveals a stacked vertical list of 5 nav links below the navbar; clicking any link scrolls to its section and the dropdown closes
    - `npm run lint` reports no new errors in NavBar.jsx
  </acceptance_criteria>
  <done>Hamburger menu appears on mobile widths, all 5 links scroll to their section and close the menu, desktop nav unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ContactMe layout + hygiene (BUG-02, BUG-05, BUG-08)</name>
  <files>src/components/ContactMe.jsx</files>
  <read_first>
    - src/components/ContactMe.jsx (current state — the duplicate container at lines 77-79, form column lines 80-150, hidden input line 142, console.log lines 30 and 40)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (ContactMe section — exact duplicate-div structure, right-column JSX, and external-link pattern)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (Pitfall 2 and Pitfall 6 — flex layout and hardcoded contact info rationale)
    - src/components/ProjectCard.jsx (lines 21-22 — the established `target="_blank" rel="noopener noreferrer"` link pattern to copy)
  </read_first>
  <action>
    Remove the duplicate inner container: the outer `<div className="relative z-10 container mx-auto px-4 flex flex-col md:flex-row">` at line 77 is correct; delete the IDENTICAL duplicate `<div>` at line 79 and its matching closing `</div>` (around line 150), so the form column `<div className="md:w-1/2 mb-8 md:mb-0">` becomes a direct child of the line-77 flex container (BUG-02; Pitfall 2 — both columns must be direct children of the flex row with explicit `md:w-1/2`). Add a sibling right column `<div className="md:w-1/2 md:pl-8">` after the form column, containing an `<h2 className="text-3xl font-bold mb-6 text-white">Get in touch</h2>` and a `text-white space-y-4` block with: a plain `<p>` showing email `antonilueddeke@gmail.com`, a LinkedIn `<a href="https://linkedin.com/in/antonilueddeke">`, and a GitHub `<a href="https://github.com/alueddeke">` — both anchors use `target="_blank" rel="noopener noreferrer" className="underline"` (copying ProjectCard's safe-link pattern). Hardcode this contact info for Phase 1; the `contact` prop's Contentful shape is unknown and binding is deferred to Phase 2 (Pitfall 6, Assumption A2). Replace the BUG-08 hidden input `value="Your Name"` with `value="Antoni Lueddeke"` (line 142). Delete the two BUG-05 console.log lines (line 30 success handler, line 40 failure handler); leave the surrounding `.then()/.catch()`, state updates, and any console.error untouched.
  </action>
  <verify>
    <automated>grep -c "console.log" src/components/ContactMe.jsx | grep -qx 0 && grep -q 'value="Antoni Lueddeke"' src/components/ContactMe.jsx && grep -c "md:w-1/2" src/components/ContactMe.jsx | grep -qx 2 && grep -q "github.com/alueddeke" src/components/ContactMe.jsx && npm run lint 2>&1 | grep -v node_modules | grep -i "ContactMe.*error" | wc -l | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "console.log" src/components/ContactMe.jsx` returns 0
    - ContactMe.jsx contains `value="Antoni Lueddeke"` and does NOT contain `value="Your Name"`
    - `grep -c "md:w-1/2" src/components/ContactMe.jsx` returns exactly 2 (form left column + contact right column)
    - The contact right column contains the email string `antonilueddeke@gmail.com`, a LinkedIn anchor, and a GitHub anchor `https://github.com/alueddeke`, each external anchor carrying `target="_blank"` and `rel="noopener noreferrer"`
    - On desktop, the form and contact-info columns render side-by-side; on mobile they stack
    - `npm run lint` reports no new errors in ContactMe.jsx
  </acceptance_criteria>
  <done>ContactMe renders two columns, hidden input reads "Antoni Lueddeke", no console.log remains, layout stacks on mobile.</done>
</task>

<task type="auto">
  <name>Task 3: SEO/OG meta tags, footer year, App.jsx log removal (BUG-03, PERF-03, BUG-06, BUG-05)</name>
  <files>index.html, src/components/Footer.jsx, src/App.jsx</files>
  <read_first>
    - index.html (current `<head>` — only charset, icon, fonts, viewport, and `<title>Antoni Lueddeke</title>`)
    - src/components/Footer.jsx (line ~10-11 — the hardcoded `. 2024` literal inside the project credit `<p>`)
    - src/App.jsx (lines 20-35 — the useEffect fetch; console.log to delete is line 24; console.error on the catch must remain)
    - .planning/phases/01-foundation-fixes/01-PATTERNS.md (index.html section — exact meta block; Footer and App.jsx sections)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (Pitfall 3 — OG tags must be inside `<head>` with `property=` attributes)
  </read_first>
  <action>
    In index.html, replace `<title>Antoni Lueddeke</title>` with `<title>Antoni Lueddeke — Software Engineer</title>` (D-04). Immediately after, add `<meta name="description" content="Software Engineer based in Toronto — full-stack web, mobile, and AI/ML. AWS certified. Currently open to new roles." />` (D-05, PERF-03). Then add Open Graph tags inside `<head>` using `property=` attributes (Pitfall 3): `og:type`=`website`, `og:title`=`Antoni Lueddeke — Software Engineer`, `og:description`=same as the description above, `og:url`=`https://antonilueddeke.netlify.app`, `og:site_name`=`Antoni Lueddeke` (D-06, D-08; og:url and og:site_name are discretionary per D-08). Then add Twitter Card tags using `name=` attributes: `twitter:card`=`summary`, `twitter:title` and `twitter:description` matching the above. Do NOT add `og:image` or `twitter:image` (D-07 — text-only unfurl, image deferred to post-Phase-3). In Footer.jsx, replace the literal `2024` in the credit paragraph with the JSX expression `{new Date().getFullYear()}` (BUG-06; no import needed). In App.jsx, delete only line 24 `console.log(result);` between the getContentfulData await and setData; keep the `console.error(err)` in the catch block (BUG-05).
  </action>
  <verify>
    <automated>grep -q "<title>Antoni Lueddeke — Software Engineer</title>" index.html && grep -q 'property="og:title"' index.html && grep -q 'name="twitter:card"' index.html && grep -cq 'og:image' index.html; test $? -ne 0 && grep -q "new Date().getFullYear()" src/components/Footer.jsx && grep -c "console.log" src/App.jsx | grep -qx 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - index.html `<head>` contains `<title>Antoni Lueddeke — Software Engineer</title>`
    - index.html contains `<meta name="description"` with the exact D-05 content string
    - index.html contains meta tags `property="og:type"`, `property="og:title"`, `property="og:description"`, `property="og:url"`, `property="og:site_name"`
    - index.html contains `name="twitter:card"` with content `summary`, plus `twitter:title` and `twitter:description`
    - index.html contains NO `og:image` and NO `twitter:image` tag
    - `grep "2024" src/components/Footer.jsx` returns no hardcoded year; Footer.jsx contains `{new Date().getFullYear()}`
    - `grep -c "console.log" src/App.jsx` returns 0; App.jsx still contains `console.error(err)` in the catch
  </acceptance_criteria>
  <done>index.html unfurls with correct title/description/OG/Twitter tags (no image), footer year is dynamic, App.jsx has no debug log.</done>
</task>

<task type="auto">
  <name>Task 4: Verify BUG-07 compliance (no implementation)</name>
  <files></files>
  <read_first>
    - src/components/ProjectCard.jsx (lines 21-22 and 97-98 — the only two `target="_blank"` occurrences; both already carry `rel="noopener noreferrer"`)
  </read_first>
  <action>
    Verification-only task (BUG-07 is already satisfied per RESEARCH.md and confirmed by inspection — both `target="_blank"` links in ProjectCard.jsx already carry `rel="noopener noreferrer"`). Run the scan to confirm every `target="_blank"` in src/ has the rel attribute and that Task 2 did not introduce a non-compliant link. Make no code changes unless the scan finds a violation; if it does, add `rel="noopener noreferrer"` to the offending link.
  </action>
  <verify>
    <automated>test "$(grep -rln 'target="_blank"' src/ | wc -l | tr -d ' ')" != "0" && for f in $(grep -rln 'target="_blank"' src/); do grep -q 'rel="noopener noreferrer"' "$f" || { echo "VIOLATION in $f"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Every file in src/ containing `target="_blank"` also contains `rel="noopener noreferrer"`
    - The scan exits PASS with zero violations reported (covers ProjectCard.jsx and the new ContactMe.jsx right-column links)
  </acceptance_criteria>
  <done>All external `target="_blank"` links across src/ carry rel="noopener noreferrer"; no violations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile hamburger nav, two-column ContactMe, OG/SEO meta tags, dynamic footer year, and removal of all console.log output — across NavBar.jsx, ContactMe.jsx, Footer.jsx, App.jsx, and index.html.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the dev URL.
    2. Open DevTools → Console. Reload the page. Confirm ZERO console output appears (BUG-05).
    3. Resize the browser to a narrow phone width (< 768px) OR use device emulation. Confirm a hamburger glyph appears far-right in the navbar. Click it → a stacked list of nav links drops down. Click "Projects" → the page scrolls to Projects AND the menu closes (BUG-01).
    4. Scroll to the Contact section. Confirm you see the form on the left and a "Get in touch" contact block (email, LinkedIn, GitHub) on the right on desktop width; confirm they stack on mobile width (BUG-02).
    5. Scroll to the footer. Confirm the year reads 2026 (current year), not 2024 (BUG-06).
    6. Submit the contact form (or inspect the form) and confirm no console output and the hidden to_name carries "Antoni Lueddeke" (BUG-05, BUG-08).
    7. View page source (Ctrl/Cmd+U) and confirm the `<title>` reads "Antoni Lueddeke — Software Engineer" and OG/Twitter meta tags are present (BUG-03, PERF-03).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (e.g., menu doesn't close, right column missing, console still logging).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recruiter browser → static index.html | Untrusted client reads the static HTML shell and meta tags (no input crosses here) |
| contact form → EmailJS API | User-supplied form fields cross to a third-party email service (existing flow, unchanged) |
| external `target="_blank"` links → third-party sites | New tab gains a window.opener handle unless mitigated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | New ContactMe right-column external links (LinkedIn, GitHub) | mitigate | All new `target="_blank"` anchors include `rel="noopener noreferrer"` (Task 2); BUG-07 scan in Task 4 enforces no regression |
| T-01-02 | Information Disclosure | console.log of full Contentful response in production (App.jsx) | mitigate | Remove debug logging (Task 3); eliminates leaking API response shape to the production console |
| T-01-03 | Spoofing | OG/meta tags in shared link previews | accept | Text-only meta tags reflect public info already on the site; no secrets, no image, low value to an attacker |
| T-01-04 | Input Validation | ContactMe form submission | accept | Existing `validateEmail` regex is unchanged by this plan; no new input surface introduced |
</threat_model>

<verification>
- `grep -rn 'console.log' src/` returns zero results across the whole src tree
- Every `target="_blank"` in src/ carries `rel="noopener noreferrer"`
- index.html contains the locked title, description, OG, and Twitter Card tags and NO og:image
- `npm run lint` reports no new errors in the five modified files
- Manual QA checkpoint passes all 6 verification steps
</verification>

<success_criteria>
- ROADMAP SC-1 (mobile hamburger navigation) satisfied
- ROADMAP SC-2 (URL unfurl with title + description) satisfied
- ROADMAP SC-4 (ContactMe two-column render) satisfied
- ROADMAP SC-5 (zero console output) satisfied
- BUG-06 (dynamic footer year) and BUG-08 (correct hidden input) satisfied
- BUG-07 confirmed already compliant with no regression
</success_criteria>

<output>
Create `.planning/phases/01-foundation-fixes/01-01-SUMMARY.md` when done
</output>
