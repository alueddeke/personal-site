---
phase: 01-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml"
autonomous: false
requirements: [BUG-04]

user_setup:
  - service: netlify
    why: "Deploy the updated Gist AI Next.js app and host the live link recruiters click"
    env_vars:
      - name: FIREBASE_ADMIN_PRIVATE_KEY
        source: "gistAI .env.local — multiline PEM; paste exactly as it appears (literal \\n sequences) into Netlify env UI"
    dashboard_config:
      - task: "Set all 13 environment variables from .env.local on the new Netlify site"
        location: "Netlify Dashboard → Site configuration → Environment variables"
      - task: "Add the new Netlify site URL to Firebase Authentication authorized domains"
        location: "Firebase Console → Authentication → Settings → Authorized domains"
  - service: contentful
    why: "Update the Gist AI project card live-link URL to the new Netlify URL"
    dashboard_config:
      - task: "Edit the Gist AI project entry's live-link/URL field to the new Netlify URL"
        location: "Contentful → project entry for Gist AI"

must_haves:
  truths:
    - "A new Netlify site hosts the current gistAI Next.js app (not the old capstone frontend)"
    - "Opening the new Netlify URL loads the Gist AI app and a prompt comparison streams successfully end-to-end"
    - "The Gist AI project card on the personal website links to the new working Netlify URL"
  artifacts:
    - path: "/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml"
      provides: "Next.js build + plugin config for Netlify"
      contains: "@netlify/plugin-nextjs"
  key_links:
    - from: "Netlify build"
      to: "gistAI server env (Zod-validated)"
      via: "13 env vars set in Netlify dashboard before first deploy"
      pattern: "FIREBASE_ADMIN_PRIVATE_KEY"
    - from: "Contentful Gist AI project card URL field"
      to: "new Netlify deploy URL"
      via: "manual Contentful edit"
---

<objective>
Deploy the current Gist AI Next.js app to a working Netlify site and point the personal website's Gist AI project card at the new live URL. After this plan, a recruiter clicking the Gist AI "live link" lands on a working, streaming deployed app instead of the stale capstone frontend.

Purpose: This is the "working live link" vertical slice — the single most visible broken promise on the portfolio (a project card whose live link is dead/stale).
Output: A netlify.toml in the gistAI repo, a new live Netlify deployment, and an updated Contentful project URL.

Note: This plan operates on a SEPARATE repository at /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI — NOT the personal-website repo. It does not touch any personal-website source file, so it has zero file overlap with Plan 01 and runs in Wave 1 in parallel with Plan 01; both plans pause for the user at their blocking checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-fixes/01-CONTEXT.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md

<interfaces>
<!-- Gist AI deployment facts confirmed by inspection and RESEARCH.md BUG-04 section. -->

Repo: /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI
- Next.js 16 App Router app; `npm run build` runs `next build`
- NO `.netlify/` dir and NO `netlify.toml` — repo is NOT linked to any Netlify site
- The existing `gist-antonilueddeke.netlify.app` site is linked to a DIFFERENT repo (old capstone) — do NOT reuse it; create a NEW site (Assumption A4, Open Question 2)
- `.env.local` is present and contains 13 live keys; it must NOT be committed
- `src/proxy.ts` replaced `middleware.ts` for Next.js 16; `middleware.ts` is deleted locally but tracked in HEAD
- `env.server.ts` Zod-validates ALL server env vars at startup → missing vars = failed build (Pitfall 4)

Required netlify.toml:
  [build] command = "npm run build", publish = ".next"
  [[plugins]] package = "@netlify/plugin-nextjs"

13 env vars to set in Netlify dashboard (from .env.local):
  OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY, GROQ_API_KEY,
  UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN,
  FIREBASE_ADMIN_PROJECT_ID, FIREBASE_ADMIN_CLIENT_EMAIL, FIREBASE_ADMIN_PRIVATE_KEY,
  NEXT_PUBLIC_FIREBASE_API_KEY, NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
  NEXT_PUBLIC_FIREBASE_PROJECT_ID, NEXT_PUBLIC_FIREBASE_APP_ID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read handoff, prepare repo, create netlify.toml and new Netlify site</name>
  <files>/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml</files>
  <read_first>
    - /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/AGENT_HANDOFF.md (D-12 — definitive architecture + recent-changes briefing; read before doing anything)
    - /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/package.json (build script + dependency state)
    - /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/.env.local (the 13 env var names/values to transcribe to Netlify — DO NOT commit this file)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (BUG-04 section + Pitfalls 4, 5)
  </read_first>
  <action>
    Working inside `/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI`. First read AGENT_HANDOFF.md fully (D-12). Confirm the deployment gap with `netlify status` (expected: "not linked to a site"). Handle the tracked-but-deleted `middleware.ts`: since `src/proxy.ts` replaces it under Next.js 16, commit the deletion of `middleware.ts` so the deploy builds from a clean tree (Assumption A6). Create `netlify.toml` at the repo root with `[build]` `command = "npm run build"` and `publish = ".next"`, plus a `[[plugins]]` entry `package = "@netlify/plugin-nextjs"` (per RESEARCH.md — Netlify Next.js 16 support is confirmed). Create a NEW Netlify site (do NOT relink the old `gist-antonilueddeke` site — Open Question 2 / Assumption A4) via `netlify sites:create` and link the repo. Do NOT deploy yet — env vars must be set first (Task 2). If any Netlify CLI command returns an auth error, surface it as a blocking auth gate rather than guessing.
  </action>
  <verify>
    <automated>test -f "/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml" && grep -q "@netlify/plugin-nextjs" "/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml" && grep -q 'publish = ".next"' "/Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - AGENT_HANDOFF.md has been read (its recent-changes guidance is reflected in deploy decisions)
    - `netlify.toml` exists at the gistAI repo root containing `command = "npm run build"`, `publish = ".next"`, and `package = "@netlify/plugin-nextjs"`
    - A NEW Netlify site exists and the gistAI repo is linked to it (`netlify status` now reports a linked site that is NOT the old capstone site)
    - `middleware.ts` deletion is committed (`git status` in gistAI shows no pending middleware.ts deletion)
    - `.env.local` is NOT staged or committed
  </acceptance_criteria>
  <done>gistAI repo has netlify.toml, is linked to a new Netlify site, working tree is clean, no deploy attempted yet.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>A new Netlify site linked to the gistAI repo, awaiting environment variables before its first build.</what-built>
  <how-to-verify>
    The 13 environment variables (live API keys + Firebase admin credentials) must be set in the Netlify dashboard before the first deploy, because `env.server.ts` Zod-validates all server vars at build time and the build fails if any are missing (Pitfall 4). These contain live secrets and cannot be committed to git, so they must be entered through the Netlify UI by you.

    1. Open the new site in the Netlify Dashboard → Site configuration → Environment variables.
    2. From gistAI/.env.local, add all 13 variables: OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY, GROQ_API_KEY, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, FIREBASE_ADMIN_PROJECT_ID, FIREBASE_ADMIN_CLIENT_EMAIL, FIREBASE_ADMIN_PRIVATE_KEY, NEXT_PUBLIC_FIREBASE_API_KEY, NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN, NEXT_PUBLIC_FIREBASE_PROJECT_ID, NEXT_PUBLIC_FIREBASE_APP_ID.
    3. For FIREBASE_ADMIN_PRIVATE_KEY, paste the value EXACTLY as it appears in .env.local (with literal `\n` sequences inside the PEM) — the app's admin.ts does `.replace(/\\n/g, '\n')` at runtime (Pitfall 5).
    4. Confirm with `netlify env:list` that all 13 names are present.
  </how-to-verify>
  <resume-signal>Type "env set" once `netlify env:list` shows all 13 variables, or describe any blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Build and deploy gistAI to production Netlify</name>
  <files></files>
  <read_first>
    - /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI/netlify.toml (just created)
    - .planning/phases/01-foundation-fixes/01-RESEARCH.md (Pitfalls 4 and 5 — env validation and Firebase private key newlines)
  </read_first>
  <action>
    Inside the gistAI repo. First confirm `netlify env:list` shows all 13 variables (the env-set checkpoint must have completed). Run a local `npm run build` to catch build errors before deploying; if it fails on a ZodError, the env vars are the cause (Pitfall 4) — report which var is missing rather than proceeding. Once the local build is clean, deploy to production with `netlify deploy --build --prod`. Capture the resulting production URL from the deploy output. If the deploy succeeds but runtime calls fail, check `/api/health` (basic routing) vs `/api/library` (Firebase admin) to localize a Firebase private-key newline problem (Pitfall 5). Surface any Netlify auth error as a blocking gate.
  </action>
  <verify>
    <automated>cd /Users/antonilueddeke/Desktop/Projects/interview-prep/Old/gistAI && netlify env:list 2>/dev/null | grep -Eq 'FIREBASE_ADMIN_PRIVATE_KEY' && echo "ENV-OK (manual deploy verification follows in checkpoint)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes locally with no ZodError and no build failure
    - `netlify deploy --build --prod` completes and emits a production URL
    - The production URL returns HTTP 200 (the new Next.js app, confirmed not the old capstone "gist" title)
  </acceptance_criteria>
  <done>gistAI is deployed to production on the new Netlify site and the live URL is captured for the next steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The current Gist AI Next.js app deployed to a new production Netlify URL.</what-built>
  <how-to-verify>
    1. Open the new Netlify production URL in a browser.
    2. Confirm the current Gist AI app loads (not the old capstone UI).
    3. Run a prompt comparison end-to-end: submit a prompt and confirm responses STREAM in and any speed/comparison UI animates (BUG-04, ROADMAP SC-3).
    4. If you use Google Sign-in, confirm it works — if it fails with an unauthorized-domain error, add the new Netlify URL to Firebase Console → Authentication → Authorized domains, then retry.
    5. Confirm no 500s on the core API routes (a 500 on /api/library with 200 on /api/health indicates a Firebase private-key newline issue — Pitfall 5).
  </how-to-verify>
  <resume-signal>Type "live works" once the deployed app streams a comparison end-to-end, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update Contentful Gist AI project card URL</name>
  <files></files>
  <read_first>
    - .planning/phases/01-foundation-fixes/01-CONTEXT.md (D-11 step 5 — update Contentful project card URL to the working Netlify URL)
  </read_first>
  <action>
    Update the Gist AI project entry's live-link/URL field in Contentful to the new verified Netlify production URL from Task 2 (D-11 step 5). If Contentful write access is available via CLI/API with credentials in the environment, perform the update programmatically; otherwise surface a blocking human-action checkpoint instructing the user to edit the Gist AI project entry's URL field in the Contentful web app. Do not hardcode the URL anywhere in the personal-website JSX — the project card URL must come from Contentful (per CLAUDE.md: all dynamic content goes through Contentful).
  </action>
  <verify>
    <automated>echo "Contentful update verified via the human-verify checkpoint below (CMS state is not gitted)"</automated>
  </verify>
  <acceptance_criteria>
    - The Gist AI project entry in Contentful has its live-link/URL field set to the new Netlify production URL
    - No hardcoded Gist AI URL was added to any personal-website source file
  </acceptance_criteria>
  <done>Contentful Gist AI project card points at the new working Netlify URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Gist AI project card URL in Contentful updated to the new live Netlify deployment.</what-built>
  <how-to-verify>
    1. Open the personal website (dev or production) and scroll to the Projects section.
    2. Click the Gist AI project card's live link.
    3. Confirm it opens the NEW working Netlify deployment (the current app that streams), not the old capstone UI or a dead link (ROADMAP SC-3).
  </how-to-verify>
  <resume-signal>Type "approved" once the project card link opens the working live app, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local secrets → Netlify dashboard | 13 live API keys / Firebase admin credentials cross from local file to Netlify's encrypted env store |
| git repo → remote | Secrets must never cross into version control |
| recruiter browser → gistAI Netlify functions | Public users invoke server routes backed by paid AI APIs and Firebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/Netlify plugin installs during build | mitigate | Only `@netlify/plugin-nextjs` (first-party Netlify plugin) is added; no new application packages introduced. No [ASSUMED]/[SUS] packages per RESEARCH.md Package Legitimacy Audit |
| T-01-05 | Information Disclosure | .env.local with live API keys | mitigate | Never commit .env.local; secrets entered only through Netlify env UI (Task 1 keeps it unstaged; checkpoint sets vars via dashboard) |
| T-01-06 | Spoofing | Firebase Auth on the new domain | mitigate | New Netlify URL added to Firebase authorized domains so Google Sign-in works only on the intended origin (checkpoint step 4) |
| T-01-07 | Denial of Service | Public AI-backed API routes | accept | Existing Upstash Redis rate limiting (from gistAI app, unchanged) governs abuse; no new exposure introduced by this deploy |
</threat_model>

<verification>
- `netlify.toml` exists with the Next.js plugin and `.next` publish dir
- `netlify env:list` shows all 13 required variables before deploy
- Production URL returns 200 and streams a comparison end-to-end
- Contentful Gist AI project card URL points at the new Netlify URL
- `.env.local` is not committed (git log/status clean of secrets)
</verification>

<success_criteria>
- ROADMAP SC-3 satisfied: the Gist AI project card live link opens a working deployed app on Netlify
- BUG-04 satisfied: the current gistAI app is deployed, verified end-to-end, and linked from Contentful
</success_criteria>

<output>
Create `.planning/phases/01-foundation-fixes/01-02-SUMMARY.md` when done
</output>
